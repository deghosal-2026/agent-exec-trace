/**
 * Test setup file executed before each test suite.
 *
 * Imports `@testing-library/jest-dom/vitest` to add custom DOM matchers
 * (e.g. `toBeInTheDocument`, `toHaveTextContent`) to Vitest's `expect`.
 * This setup is configured in `vitest.config.ts` via the `setupFiles` option.
 *
 * @see https://github.com/testing-library/jest-dom
 */

import "@testing-library/jest-dom/vitest";