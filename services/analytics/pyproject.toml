[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-exec-trace-analytics"
version = "0.2.0"
description = "Behavior analytics service for agent-exec-trace - transforms raw traces into operator-ready signals"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "opentelemetry-api>=1.24",
    "opentelemetry-sdk>=1.24",
    "httpx>=0.27",
    "asyncpg>=0.29",
    "pydantic>=2.5",
    "pydantic-settings>=2.1",
    "click>=8.1",
    "alembic>=1.13",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov>=5",
    "pytest-asyncio>=0.24",
    "ruff>=0.9",
    "mypy>=1.13",
    "datasets>=3.0",
    "pyarrow>=17.0",
]

[project.scripts]
analytics = "analytics.main:cli"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "N"]

[tool.ruff.lint.isort]
known-first-party = ["analytics"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --asyncio-mode=auto"
pythonpath = ["src"]

[tool.mypy]
strict = true
python_version = "3.10"
mypy_path = "src"